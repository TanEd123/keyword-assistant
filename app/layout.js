export const metadata = {
  title: "Keyword Research Bot",
  description: "Find keywords your ideal clients are searching for",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
